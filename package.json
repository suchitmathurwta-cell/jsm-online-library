{
  "name": "chetna-digital-library",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && node -e \"const fs=require('fs'); fs.rmSync('dist', {recursive:true,force:true}); fs.cpSync('client/dist', 'dist', {recursive:true});\" && npm --prefix server install",
    "start": "node server/index.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}